server:
  port: 8071

camunda:
  bpm:
    admin-user:
      id: admin
      email: admin@localhost
      first-name: Adminus
      last-name: Administratus
      password: admin
    client:
      default-serialization-format: application/json
    default-serialization-format: application/json
    login:
      enabled: true
      user-id: admin
    worker:
      task-mode: SERVICE

